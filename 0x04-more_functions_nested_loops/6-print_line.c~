#include <stdio.h>
void print_line(int);
int main(void)
{
  
print_line(4);
return (0);
}
void print_line(int n)
{
  int r;
  
  if (n > 0)
    {
      for (r = 0; r < n; r++)
	
      putchar('_');
    }
  else
    {
    putchar('\n');

    }
}
